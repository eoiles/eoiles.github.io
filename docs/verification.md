# 实际验证记录

验证日期：2026-09-17；精简版复核：2026-09-18。Windows 本机，Node.js 20.17.0，Vite 6.4.3；Playwright 使用已安装的 Google Chrome 和下载的 WebKit 26.6。手机是浏览器设备/视口模拟，不能等同于真实 iOS/Android 系统。

## 协议

`npm test`：40 项全部通过。

- 用户要求的 A、中、😀 三个精确向量。
- 全部 256 个字节：独立位移公式、正向排列、逆排列。
- 八个单置位字节：左列从上到下，再右列从上到下；在浏览器 DOM 上再检查实际 SVG 坐标。
- 历史原生格式独立向量与全字节测试。
- 空文本、首尾空格、制表符、LF、CRLF、单 CR、组合字符、ZWJ 家庭与职业 emoji、代理码元、BOM/变体选择符。
- 非法字符位置、奇数长度、前后 U+2800、主动空白清理不删除 U+2800。
- 全部 65,536 个 UTF-16 码元在两种格式下往返一致。

## 浏览器

在原仓库生成的生产构建 `dist` 上，桌面 Chrome、Pixel 7 配置的移动 Chrome、iPhone 13 配置的 WebKit 共 **54 项功能/无障碍检查通过**。其中 53 项在完整生产回归中直接通过；WebKit 双主题无障碍检查触及默认 30 秒测试时限，调整该项预算至 60 秒后单独复核，39.5 秒完成且没有违例。未更改应用代码来规避检查。手动复制回退也验证了原始 CRLF 的保留。

另外生成了 10 张深浅主题/不同宽度截图和一段约 13 秒的实际交互录屏；非桌面视频记录任务按设计跳过。

`npm run build`、TypeScript 类型检查、`git diff --check` 均通过；`/` 和 `/index.html` 的生产入口正常，`/index.html` HTTP 状态为 200。

覆盖：首次打开不聚焦、即时转换、连续编辑、最近编辑侧、模拟 compositionstart/input/compositionend、点阵与黑白格共用节点、格式保护、清空与示例撤销、错误修正、奇数长度、主动清理与恢复、剪贴板成功与失败、CRLF 粘贴后继续编辑、长输入与旧结果竞争、仅偏好持久化、Worker 与存储失败回退、主题跟随系统、主动教学和减少动态效果、编辑器选区/焦点/内部滚动保持。

导出验证：SVG 包含每个完整单元和全部 bit；2,200 个盲文字符的 SVG 不受 256 个预览上限影响。PNG 检查实际尺寸、文件签名，以及首白/黑模块/尾白的 RGBA 像素。超出安全画布时明确报错而非裁切。

自动无障碍：axe WCAG 2 A/AA、WCAG 2.1 AA；深浅主题、有效预览和展开的帮助/设置/源码、错误状态，无报告违例。发现的浅色英文小标对比度不足已修复。这不能替代真人读屏测试。

## 布局和实际观察

- 320×568、375×812、430×932、844×390 横屏、375×360 缩小视口：无 document 横向溢出。
- 检查编辑字体至少 16px、手机复制按钮触控高度、底部栏在缩小视口内可见。
- 在 Codex 内置浏览器实际输入中文与 emoji、复制、切换黑白格、展开帮助，并确认原文焦点仍在。
- 逐张查看桌面深色、手机浅色截图。另生成桌面空状态、浅色、黑白格，以及 320/375/430 的深浅主题截图。
- 短录屏实际展示输入、复制成功、同坐标形态切换、清空撤销、主动阅读教学。

## 未在本机验证

- 真实 iPhone 的 Safari、真实 Android 的 Chrome 和系统软键盘升降/安全区；本次使用引擎与视口模拟。
- 真正的系统中文输入法候选窗；自动检查的是组合事件与选区行为，不能代替系统 IME 实测。
- VoiceOver、TalkBack 或 NVDA 真人听读。
- GitHub Pages 线上工作流运行及远程设置；本次未推送和部署。

代码已包含 VisualViewport、动态视口、安全区与 composition 事件支持。上述未验证项保留为上线前真机复核清单，不声称已经验证。


## 2026-09-18 界面精简复核

移除大中小尺寸选择、英文编号、大幅装饰区、重复预览说明与操作通知面板。工作区合为一个整体；图像导出收进菜单，辅助网格收进教学。手机复制栏仅在主复制按钮不可见时出现。示例、清空、撤销和格式切换复用状态行；两种预览的高度保持一致。

- 生产构建、类型检查、40 项协议测试和 `git diff --check` 通过。
- 三个浏览器配置合计 **57 项功能/无障碍用例均有通过记录**（全量回归加针对修订与失败项的复核，并非一次无重试运行）。旧测试中的「手机复制栏始终可见」假设已改为按可见区域验证；补充导出菜单鼠标/键盘、Escape、焦点和选区、复制栏出现与收起、状态行撤销、切换前后高度一致性。
- Windows WebKit 曾在导航、驱动查询、轮询断言阶段超时；trace 显示一次尺寸查询在约 12 秒后返回相同高度，没有数值不匹配。WebKit 单项预算改为 60 秒，断言预算 15 秒后复核相关项。不改变应用转换和动画时序。真实 Safari 手感仍需真机检查。
- 10 张截图及短录屏均更新为精简版。桌面和手机深浅主题已目视检查。
- `src/core.js` 与上一版逐字节一致，SHA-256：`63df7df7cb739db21d176e69a8c08183a2703fde02e8cdc6af3ae7df94482ed2`。


## 2026-09-19 整体重设计与动效验证

本轮调整了桌面编辑/预览的整体布局、手机布局、深浅配色、格式设置、交互控件和动效。`src/core.js` 的 SHA-256 与上一版一致。

- 生产构建、类型检查、40 项协议测试、`git diff --check` 通过。
- 桌面/移动 Chromium 的 38 项既有功能与无障碍用例均有通过记录。首次测试发现手机浮栏入场时越出视口约 1px，调整入场方向后，该项在三个浏览器配置中复核通过。
- 新增三种浏览器下的 9 项动效/渲染检查：增量编辑保留旧节点并逐位核对数据；快速连续开合菜单与帮助、焦点选区不丢失；减少动态效果在操作途中生效并完成到正确状态。
- WebKit 另完成浮动复制栏与导出菜单、点阵形态/实际坐标、格式与撤销、剪贴板、SVG/PNG 完整性、响应式尺寸、双主题 WCAG AA 等 7 项复核。共 54 个不同浏览器用例有通过记录，包含全量 Chromium 回归与针对性 WebKit 复核；没有宣称本轮重跑了全部 WebKit 用例。
- 本轮 axe 检查无报告违例。仍非真人读屏验证。
- 新版 11 张截图（新增展开设置）与约 17 秒交互录屏已更新。检查桌面暗色、设置界面及手机浅色完整截图；实际在内置浏览器输入示例、切换主题和查看设置。

真实手机软键盘、真正的中文 IME 候选窗、真人读屏及 GitHub Pages 线上发布仍未验证。本轮没有推送或部署线上。


## 2026-09-19 转换工具布局复核

本轮按用户反馈，将原文/编码/独立可视化的堆叠改为原文与单一结果区。桌面对照，手机纵向两区；结果提供编码、点阵、黑白格原位切换。没有修改 `src/core.js`，SHA-256 仍为 `63DF7DF7CB739DB21D176E69A8C08183A2703FDE02E8CDC6AF3AE7DF94482ED2`。

- 构建、TypeScript 检查、40 项核心协议测试、`git diff --check` 通过。
- 桌面/移动 Chromium 的 48 个浏览器用例均有通过记录：首次整轮 46 项通过，响应式用例发现复制按钮位于视口边界时，悬停位移/子像素取整造成约 1px 差异；修正判定后两个配置复测通过。
- 针对最终布局，三种浏览器配置共 15 项复测通过，覆盖真实八位视觉坐标、视口变化、复制栏、320/375/430px 首屏、结果区高度稳定、编码选区/滚动/解码源保留。包含 WebKit 的 5 项。本轮共 53 个不同浏览器用例有通过记录；未宣称完整重跑 WebKit 全套。
- 测试断言在 320×568、375×812、430×932 下，原文输入、主复制按钮和结果正文均在首屏内；切换三种视图不移动结果区域。另覆盖 844×390 横屏和 375×360 键盘大小视口。
- 本轮 Chromium 的双主题 axe 检查无报告违例；复制、清空撤销、非法编码恢复、IME 组合事件模拟、PNG/SVG 完整性与长输入测试通过。
- 手机 SVG 预览使用 8px 模块，桌面 14px。视觉位置断言按单格尺寸归一化，仍独立检查左列自上而下、右列自上而下的八种单一置位位置；导出尺寸保持原规则。

实际内置浏览器检查了手机与桌面，输入、复制反馈、三种视图及重新编辑入口；截图和交互录屏随最终布局更新。真实 iOS/Android 软键盘、系统 IME 候选窗与真人读屏仍未验证；本轮无线上发布。


## 2026-09-19 编码方式与读图逻辑修正

本节为最新验证记录；前面各节中的「未发布」仅描述当时的迭代状态。此前转换工具版本已通过 GitHub Actions 发布，Pages 已使用构建产物部署。

- 40 项协议测试、TypeScript 检查、生产构建通过。核心 SHA-256 仍为 `63DF7DF7CB739DB21D176E69A8C08183A2703FDE02E8CDC6AF3AE7DF94482ED2`，本轮没有修改核心。
- 桌面 Chrome 与移动 Chromium 视口的 62 项功能、布局、动效与无障碍检查一次全部通过（截图用例默认跳过）。
- WebKit 首轮 6 项新用例通过，选字检查发现 `Intl.Segments.containing()` 在中文邻接 emoji 时产生重复边界。改为按需迭代、缓存已访问边界后，空示例、组合字符/ZWJ/emoji 前后选择、长输入分别在三个配置复测通过。本轮共 69 个不同的功能/无障碍浏览器用例有通过记录；没有宣称完整重跑 WebKit 的历史用例。
- 阅读位置独立断言：自然顺序为左上向下再右列；原生从右下开始，物理索引为 `[7,3,6,5,4,2,1,0]`。两种格式均逐步检查全部 16 步的圆点位置、二进制高亮及值，并检查八个单一置位字节的真实位置。
- 新验证覆盖：格式点击即生效；原文驱动与编码驱动分别保留编辑侧；撤销恢复格式和内容；输入焦点与选区保留；错误时教学沿用上次有效结果的真实格式；快速切换和长输入不会被旧结果覆盖。
- 空状态 A / 中 / 😀 都可直接选，示例不改动原文/编码。已有文字用实际字选择，emoji、组合符、ZWJ 序列不拆成不可见选项，多组点阵可前后查看。选字 DOM 始终只包含附近少量字符。
- 源码默认折叠，着色前后的文本与实际核心一致；原生模式展示实际协议外围的编码/解码实现。320、375、430px 下没有页面横向溢出，源码没有双向内嵌滚动条。深浅主题源码高亮和展开帮助均纳入 axe AA 检查，无报告违例。

实际在 Edge 中打开本地新版，检查了模式选择、空示例和 Unicode 右下角起读。三种浏览器配置的自然顺序、原生顺序和源码截图另行保存。此前保留的复制/导出、首屏、视口缩小、IME 组合事件、清空撤销、输入错误、CRLF 与隐私回归在本轮 Chrome 检查中通过。

限制仍包括真实 iPhone Safari / Android Chrome 软键盘、系统中文 IME 候选窗和真人读屏；浏览器引擎与组合事件模拟不等于真机验收。本轮提交及线上发布结果见 GitHub Actions 和交付目录中的发布记录。


## 2026-09-20 精简原理展示

将展开后整段 JavaScript 实现改成 5 行 Python 概念示意，取消提交编号、文件路径、技术备注和嵌套展开。实际转换核心和协议文件没有修改。

- Python 示例已实际执行，检查 A 的 16 位二进制、实空点序列与还原文字一致。
- 生产构建、类型检查、40 项协议测试、`git diff --check` 通过。
- Chrome、移动 Chromium 视口、WebKit 的 9 项功能/无障碍用例均有通过记录：基本双向转换、短 Python 展示和着色、320/375/430px 下无溢出、两种格式说明、深浅主题 axe AA。
- WebKit 首轮发现滚动到帮助标题时可能被底部复制栏遮挡；增加滚动留白后，三个配置的双主题无障碍用例全部复测通过。
- 三种配置各保存深浅两张原理区截图。展示只保留 A 的核心概念，并非完整 Python 协议实现；浏览器仍使用原有 JavaScript 核心。

本轮没有重新全量运行此前所有浏览器测试。真实手机软键盘、系统输入法候选窗和真人读屏的未验证边界保持不变。

## 2026-09-20 两种模式的真实示例与数字对照

- 自然顺序与原生模式分别显示对应的短 Python 编码/还原代码；不是共享的概念占位。示例明确只演示 A 的低字节 65，完整协议核心未改动。
- tests/concept.test.py 实际执行两份示例，各覆盖全部 256 个字节，对照独立真实点位掩码和实际 JavaScript 编码，再检查逆变换。两种模式均通过。
- 40 项协议测试、生产构建、类型检查通过。
- 桌面 Chrome、移动 Chromium 视口和 WebKit 的 6 项模式展示/响应式/无障碍用例全部通过；另有 3 项截图记录任务通过，输出两种模式的深浅主题截图共 12 张。
- 页面验证了切换时代码和结果小标改变，切回恢复；73654210、76514320、10240 与 256 均可见。320、375、430px 无横向溢出，深浅主题 axe AA 无报告违例。
- 本轮未重跑全部历史浏览器用例；真实手机键盘、系统中文输入法候选窗和真人读屏仍未实测。
